# Как добавлять статьи в EcoPlace.by

## 📋 Быстрый старт

### 1. Установите необходимое ПО
```bash
# Установите Ruby и Bundler
sudo apt install ruby ruby-dev bundler

# Или на macOS
brew install ruby
